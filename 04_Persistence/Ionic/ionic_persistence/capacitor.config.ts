import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_persistence',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
